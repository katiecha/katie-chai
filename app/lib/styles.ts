// Single source for the bordered-panel chrome shared by cards across the
// site. Consumers append their own padding, layout, and transition classes.
export const CARD_SHELL = "border border-border rounded-fillet"
export const CARD_SHELL_HOVER = `${CARD_SHELL} hover:border-border-hover`
